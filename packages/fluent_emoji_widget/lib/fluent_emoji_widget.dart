import 'package:flutter/widgets.dart';

class FluentEmojiWidget extends StatelessWidget {
  const FluentEmojiWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}